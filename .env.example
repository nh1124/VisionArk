# AI TaskManagement OS - Environment Configuration
# Copy this file to .env and fill in the values

# =========================
# Required: API Keys
# =========================
GEMINI_API_KEY=your_gemini_api_key_here

# =========================
# Server Settings
# =========================
BACKEND_PORT=8000
FRONTEND_PORT=3000

# =========================
# Database (PostgreSQL - REQUIRED)
# =========================
# For Docker: leave empty (docker-compose sets it automatically)
# For local PostgreSQL:
DATABASE_URL=postgresql://atmos:atmos_secret@localhost:5432/atmos

# Docker PostgreSQL settings
POSTGRES_USER=atmos
POSTGRES_PASSWORD=atmos_secret
POSTGRES_DB=atmos

# =========================
# Authentication (Phase 1 - Session/JWT)
# =========================
ATMOS_ENV=dev                    # dev | prod
HOST=0.0.0.0                     # 0.0.0.0 for Docker, 127.0.0.1 for local dev

# JWT Token Settings (CHANGE IN PRODUCTION!)
JWT_SECRET_KEY=your_jwt_secret_key_here_min_32_chars_change_this
JWT_EXPIRE_MINUTES=1440          # 24 hours (60 * 24)

# Legacy API Key Settings (Phase 2, not used yet)
# ATMOS_REQUIRE_API_KEY=false
# ATMOS_API_KEY_PEPPER=dev_pepper_change_in_prod

# =========================
# Optional: OpenAI (alternative LLM)
# =========================
# OPENAI_API_KEY=your_openai_key_here
